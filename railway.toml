[build]
  builder = "nixpacks"
  buildCommand = "composer install --no-dev --optimize-autoloader --ignore-platform-reqs && php artisan optimize"

[deploy]
  startCommand = "vendor/bin/heroku-php-apache2 public/"
