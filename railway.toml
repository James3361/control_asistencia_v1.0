[build]
  builder = "NIXPACKS"
  buildCommand = "composer install --ignore-platform-reqs --no-dev --optimize-autoloader"

[deploy]
  startCommand = "php -S 0.0.0.0:$PORT -t public"
  numReplicas = 1
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
